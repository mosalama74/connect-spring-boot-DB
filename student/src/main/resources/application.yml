server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_info
    username: mosalama
    password: mosalama
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
